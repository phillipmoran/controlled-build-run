# COVERAGE.md — every source section accounted for

Maps EVERY section of the canonical source text —
`skills/controlled-build-run/SKILL.md` (1076 lines, frozen during this
extraction) — to its destination. This map is the semantic replacement for a
line-count parity rule: an unaccounted paragraph is a blocking gap.

Dispositions:

- `core:<file>` — the law lives in that cbr-core file (verbatim where the
  content was neutral; mechanisms renamed generically where a sentence mixed
  law with a provider mechanism, with the split noted here).
- `claude-provider` — provider mechanism text; stays for the future Claude
  adapter/leaf file. Named here by source anchor, never by the primitive
  string itself (this file is inside the neutrality-linted tree by design).
- `not-ported` — with reason.
- `pending-P<n>` — row to be resolved in that phase of the extraction plan.

| Source section (SKILL.md lines) | Disposition | Notes |
|---|---|---|
| Porting header (1–11) | not-ported | Port-time metadata of the Claude leaf (origin-name mapping, blocker-file names, builder dial); superseded for core by this extraction. Leaf keeps its own. |
| Skill frontmatter (13–26) | not-ported | Leaf skill-registry metadata; core files are law documents, not a dispatchable skill. |
| What this gives you (28–38) | core:policy.md | Near-verbatim; "four automatic checks" → "automatic checks" (the count was tied to the leaf's wiring). |
| The shape — check table (40–54) | core:policy.md | Table preserved. Interactive-ask row: the blocked tool's name and the headless-builder branch pattern are claude-provider; law kept generically. Deterministic-check row: the source repo's tool names (python-toolchain formatter/linter/type-checker) generalized to format/lint/types/tests — repo toolchain, not law. |
| Gating principle (56–58) | core:policy.md | Verbatim ("deterministic facts may gate; fallible judgment may only surface"). |
| Two hook systems (60–68) | core:policy.md | Law kept (agent hooks vs git hooks; re-ground must ride an event that can inject context). The event names, settings-file path, and the log-only-event caveat's concrete names are claude-provider. |
| Phase 1 — verify or wire, intro + six pieces (70–105) | core:policy.md | CHECK-BEFORE-YOU-CLOBBER law + the six pieces, generically. Exact file paths, install commands, hook-runner invocations, and the Probity-repair skill pointer (84–86) are claude-provider. Gate-inheritance note (96–100) kept in policy piece 5, cross-referenced to modes. |
| Fail open / fail closed (107–113) | core:policy.md | Verbatim, plus the enforcement-guard exception (guard infra fails closed) lifted from the Claude leaf's live guard-config doctrine — law, so it rides in core even though the source paragraph omits it. |
| Compaction window (115–137) | core:policy.md | Principle only (compact late, threshold with headroom, doctor-checked, does not replace re-ground). The three settings keys, their values, the ratification note's numbers, and the clamp detail are claude-provider/leaf (the dial's values are a leaf decision by locked plan rule). |
| Post-compaction re-ground (139–163) | core:policy.md | Law kept whole: paste-not-point rationale, the four injected artifact classes, already-booted guard, point-at-not-paste for glossary/contracts, verify-it-targets-this-plan. The named repo docs become generic classes; the project-specific boot-ritual caveat pointer (155–158) is not-ported (port-time caveat; lives with the leaf's spec references). |
| One plan, one worktree (165–216) | core:strand.md | Strand law, why-folder-binding, 4 setup steps, every-session re-check, strand definition. Provision-command path, dep-symlink recipe, the worktree-local allowlist section, the skip-prompt dispatch flag discussion, and the model-alias caveat pointer to the leaf's harness-spec §7 are claude-provider (the folder-switch-tool caveat lives in the 479–541 and 738–982 rows, not here) (law kept: unattended builders must not stall on prompts; removing prompts must not remove hook gates). |
| Plan altitude (218–242) | core:strand.md | Near-verbatim; "Open with the operator" → "Open with the human" (person-neutral). |
| Phase 2 — run the build loop (244–426) | core:build-loop.md + core:reviews.md | Steps 1–5, 7, 9 → build-loop.md near-verbatim (incl. the commit-small LAW with its ratification date, docs-only path routing, review close-discipline with its 2026-06-12 enforcement note, and the crashed-review re-run doctrine with its ~5-crash bound). Step 6 (337–348) → reviews.md "Golden samples". Step 8 (350–407) → reviews.md "Checkpoint review" with the full taxonomy/calibration/output/dedup text; build-loop.md steps 6/8 keep one-line cross-references. Generalized: the leaf's panel-command name, the source repo's toolchain names, the reviewer CLI verbs, the person-named escalation target, and the vendor-named outage example. NORMATIVE generalization in step 9: the source's unconditional "merge the branch into main" becomes "merge into its base — main solo / integration in a fleet", harmonized with the fleet section's one-merge-to-main rule (same class as the 738–982 row's gate-inheritance harmonization). |
| Resolving a judgment call (428–477) | core:judgment.md | Triage buckets, panel recipe (success-framing law + its 2026-06-21 ratification), file-channel protocol, ratify rule — near-verbatim. The panel-command name, the interactive tool's name, the headless-builder branch pattern, and the person-named terminal marker are claude-provider (stated generically). The ask/answer file names are cross-harness convention, kept. |
| Probity gotchas (479–541) | core:build-loop.md | All 11 bullets with their scar notes and ratification dates. Generalized: the gated-tree glob, the test-runner name, the folder-switch tool's name (523). |
| The model dial (543–576) | core:policy.md ("The model dial — the principle") | Core carries: one-place-per-layer pinning, the tier-split savings law ("never from cutting effort or skipping review; correctness still outranks token cost"), cross-family review (with both rationales: blind spots + usage-window contention), explicit model/effort on every launch. claude-provider: every model name, effort value, pin location, the reviewer-availability check rider, the review-subagent temporary-tier note, and the dispatch-prompt keyword that turns on the leaf's multi-agent workflow mode. |
| Orchestrating a fleet (578–680) | core:modes/fleet.md | Stream/strand definitions, orchestrator-own-strand (+ integration-branch law, one-merge-to-main), post-merge smoke law with its four-ship-stopper scar (product names generalized), closeout/janitor law with the 17-worktrees scar, dependency-graph table (example stream names/files genericized), both edge kinds, topological dispatch, 2–3 concurrency cap with its watch-honestly rationale. The provision/closeout/janitor command paths and the example table's real component filenames are claude-provider/leaf. |
| Plan-review gate + exempt-zone law (682–723) | core:reviews.md | Extracted ahead of P4 because the plan assigns the gate to reviews.md. Mechanical criteria, panel-for-design-weighty rule (with inline-not-point-at rider from the leaf's panel-reference doc), fleet-level review, 2-round cap, exempt-zone law whole. The leaf's panel-reference file pointer and reviewer-model pin are claude-provider. |
| Fleet decision log + stream retirement (725–736) | core:modes/fleet.md | Near-verbatim (findings-as-durable-record; retire-with-findings overriding the build-loop default). |
| Orchestrated dispatch (738–982) | core:modes/fleet.md | Law kept: builder-is-real-session-root, the three invariants (on-plan / detached / watched-from-outside), companion-script subcommand roles + gather-facts-decide-nothing, prompt-layer-vs-hooks doctrine with the ~11-min-stall scar, push-firewall law, child-process death scar (overnight 2xbuilder loss), pre-trusted-dispatch law, explicit-launch law with the silent-small-model scar + wrong-model recovery ritual, gate inheritance (NOTE: the source's dispatch-section rule "cut from current main" predates and contradicts the fleet section's cut-from-integration rule; core harmonizes per the fleet section's own logic — gates flow main → integration → stream; solo cuts from main directly), dispatch-prompt contents, subagent safety boundary, the full watching-from-outside doctrine (zero-open-FAIL merge gate, ~15-min surfacing cadence, watchdog independence with the 6-hour misread scar, ground-truth liveness incl. the false-working child-shell scar and latch-on-commit-sha rule, no-context-only-state, relaunch-from-git + relaunch cap). **claude-provider remainder (= the draft of the future Claude adapter file), by source anchor:** the dispatch smoke/reground provenance notes (746–753); the companion-script path + exact subcommand invocations and needs-arm sentinel mechanics (754–789); the on-plan billing analysis, session-type flags, launch command line, and off-plan flag list (793–839); the skip-prompt flag name, the attended-run allowlist fallback, and the firewall override variable (817–838); the detached-session primitive, pty dead-ends, supervisor registry/log commands, and the isolation-guard settings rider (840–863); the trust-state file/key and blocked-state rescue specifics (865–879); the model/effort flag names, default-model inheritance caveat with model names, and the stop command (881–891); worktree hook-coverage note mechanics (893–896); registry-JSON/log-mtime/status invocations and the watch-loop shell forms (919–967). |
| Captain (984–1068) | core:modes/captain.md | N:1 law, roles doctrine (rooted-in-built-repo + small-captain + status-file artifact + wake-for-judgment with the token-waste scar), dispatch/watch/relay, watch-files-not-logs, one-watcher-line law (silence=alarm, exit on bad paths, no-overbuild rider), wake-cadence dial with its three values (process law, not provider values), foreground-watcher wedge, parked-vs-wedged discriminator (transcript tell generalized to completed-turn vs mid-flight tool call), human's-gates law. claude-provider: the watcher shell one-liner's registry command, the watch-script name + digest file path, the per-epic blocker filename convention, and the raw transcript field names (1030–1032, 1007–1009, 1019, 1060–1063). |
| (derived) modes/solo.md | core:modes/solo.md | Not a contiguous source range: assembled from the source's scattered solo statements — the fleet section's "a solo build skips this section; the single-strand rules still hold" (578–585), the captain section's "a solo build or a single small fleet skips this tier — there the one orchestrator talks to the human directly" (991–993), and the solo-merge rule implicit in the build loop's closeout step — plus the explicit restatement that all review layers gate a solo strand identically (already law in reviews.md/build-loop.md; restated, not invented). |
| The honest gap (1070–1076) | core:policy.md | Verbatim, generalized from "this skill" to "this process". |

Acceptance sources (separate files, merged in P5 → `acceptance/`):

| Source | Disposition | Notes |
|---|---|---|
| `skills/controlled-build-run/references/acceptance-checklist.md` | core:acceptance/ | Merged on this file's A–K spine: the three lean-rules, `[when]` vocabulary, definitions, and rows A1–K1 → `acceptance/checklist.md`; the completeness loop → `acceptance/mutations.md`; the six smoke scenarios → `acceptance/scenarios.md`. Provider-mechanical rows (A3 firewall mechanics, B1 hook arming, B4 write-path set, C3 threshold value, F4a–F4c launch/process/model flags, G1 registry signals, G4 not-yet-watched marker mechanics, K4 paths) marked `leaf-row` in place — the core row states the law, the leaf's acceptance supplies the mechanical row. The unbuilt set is per-leaf; core marks a row (TO BUILD in either leaf that lacks it) only where a leaf may not have built it, and each leaf tracks its own literal set. The Claude-only stale-spec aside in C1 and the J1 "currently RED" status note are leaf state, not law — leaf content. |
| Codex leaf `references/acceptance.md` (read-only from its branch) | core:acceptance/ | Codex-only rows folded into the spine: A5 second-writer refusal → A5; B2 version-pin → B1's pin clause (host-agent value = leaf); C4 already-booted note → C1; C5 threshold-clamp law → C3; its live post-merge smoke row → I2; closeout refusals/janitor → I3/I4; watcher latch/sentinel rows → G3/G4; script-law extras → K2–K4; its parity section P1–P3 → the core P-rows, with P1's 90%-line-count rule replaced by the coverage-map completeness rule (locked plan decision) and P3 generalized to "no provider primitive in core; each leaf names its OWN mechanisms". Its scenario + mutation lists merged into `scenarios.md`/`mutations.md` with provider-literal mutations described by role. |

Naming depth (dispositioned at P6): the law files keep the concrete
cross-harness tool names (Probity, RoboRev) — both run under either
harness and neither is a lint-listed primitive; `acceptance/` speaks by
role (the TDD guard, the review daemon) because its rows are the contract
template each leaf instantiates against its own tooling. A leaf that
swaps either tool renames at the law layer too; that rename is leaf work.

## Net-new law added after the extraction

This map accounts for the *extraction*: every section of the frozen source
text. Law added to core afterwards has no source row by definition, so it is
logged here instead — otherwise a reader checking "is everything accounted
for" cannot tell net-new law from an unaccounted paragraph.

| Added | Home | Why it is core, not leaf |
|---|---|---|
| Readback (2026-08-19, strand `cbr/harness-debt`) | `build-loop.md` "Readback", with the dispatcher's side in `modes/fleet.md` | A dispatched builder skimming its plan is a process failure every harness has; the check is "restate it in your own words in `progress.md`", which names no provider primitive. The leaf may add a deterministic *presence* fact; faithfulness stays judgment and may only surface (`policy.md`). |
| Zero-context plans (2026-08-19, same strand) | `strand.md` "Zero-context plans", criterion in `reviews.md` | Plans are read by agents with no conversation history under every harness — including a compacted version of their own builder. It is a plan-altitude rule, so it sits beside altitude, and it surfaces at the plan-review gate rather than gating, because "no dangling reference to a conversation" is a reviewer's judgment. |
