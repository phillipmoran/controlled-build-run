#!/usr/bin/env bash
# Rewritten commits need fresh reviews. Git supplies old/new pairs on stdin.
command -v roborev >/dev/null 2>&1 || exit 0
while read -r _old new _rest; do
  [ -n "$new" ] && roborev review "$new" >/dev/null 2>&1 || true
done
