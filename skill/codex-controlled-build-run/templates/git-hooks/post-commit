#!/usr/bin/env bash
# Queue every commit. Surfacing failure is optional; reviews are advisory per commit — the merge-path gate is the wall.
command -v roborev >/dev/null 2>&1 || exit 0
roborev review HEAD >/dev/null 2>&1 || true
